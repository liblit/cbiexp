#-*- sh -*-

sampler_builddir="@sampler_builddir@"

CC="@sampler_cc@"
CC="$CC --no-sample"
CC="$CC --sampler-scheme=branches"
CC="$CC --sampler-scheme=scalar-pairs"

# using the returns scheme masks the bug
